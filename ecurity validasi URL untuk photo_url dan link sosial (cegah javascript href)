warning: in the working copy of 'backend/server.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'frontend/js/content.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/backend/server.js b/backend/server.js[m
[1mindex 3b6eb0c..4504074 100644[m
[1m--- a/backend/server.js[m
[1m+++ b/backend/server.js[m
[36m@@ -78,6 +78,20 @@[m [mfunction adminOnly(req, res, next) {[m
   next();[m
 }[m
 [m
[32m+[m[32m// Cek URL cuma boleh http/https (atau mailto: kalau diizinkan), supaya field[m
[32m+[m[32m// seperti photo_url / link sosial tidak bisa diisi "javascript:..." (XSS).[m
[32m+[m[32mfunction isSafeUrl(value, { allowMailto = false } = {}) {[m
[32m+[m[32m  if (!value) return true; // kosong/null tetap boleh, itu ditangani validasi wajib-isi terpisah[m
[32m+[m[32m  try {[m
[32m+[m[32m    const url = new URL(value);[m
[32m+[m[32m    if (url.protocol === 'http:' || url.protocol === 'https:') return true;[m
[32m+[m[32m    if (allowMailto && url.protocol === 'mailto:') return true;[m
[32m+[m[32m    return false;[m
[32m+[m[32m  } catch {[m
[32m+[m[32m    return false; // bukan URL absolut yang valid[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 // ---------- REGISTER ----------[m
 app.post('/api/register', authLimiter, (req, res) => {[m
   const { username, password, full_name, ign, game_role, role } = req.body || {};[m
[36m@@ -143,6 +157,9 @@[m [mapp.put('/api/me', authRequired, (req, res) => {[m
   if (!full_name || !full_name.trim()) {[m
     return res.status(400).json({ error: 'Nama lengkap wajib diisi' });[m
   }[m
[32m+[m[32m  if (photo_url && !isSafeUrl(photo_url)) {[m
[32m+[m[32m    return res.status(400).json({ error: 'Link foto harus URL http/https yang valid' });[m
[32m+[m[32m  }[m
   db.prepare(`[m
     UPDATE users SET full_name = ?, ign = ?, game_role = ?, photo_url = ? WHERE id = ?[m
   `).run(full_name.trim(), ign || null, game_role || null, photo_url || null, req.user.id);[m
[36m@@ -537,10 +554,19 @@[m [mapp.put('/api/content', authRequired, adminOnly, (req, res) => {[m
 [m
   const applied = [];[m
   for (const [key, value] of Object.entries(updates)) {[m
[31m-    if (existingKeys.includes(key)) {[m
[31m-      stmt.run(String(value), key);[m
[31m-      applied.push(key);[m
[32m+[m[32m    if (!existingKeys.includes(key)) continue;[m
[32m+[m
[32m+[m[32m    // Key yang berakhiran _url dipakai sebagai href di frontend (content.js),[m
[32m+[m[32m    // jadi wajib http/https (mailto: khusus buat contact_email_url) — cegah "javascript:" XSS.[m
[32m+[m[32m    if (key.endsWith('_url')) {[m
[32m+[m[32m      const strValue = String(value || '');[m
[32m+[m[32m      if (strValue && !isSafeUrl(strValue, { allowMailto: key === 'contact_email_url' })) {[m
[32m+[m[32m        return res.status(400).json({ error: `Nilai untuk "${key}" harus link http/https yang valid` });[m
[32m+[m[32m      }[m
     }[m
[32m+[m
[32m+[m[32m    stmt.run(String(value), key);[m
[32m+[m[32m    applied.push(key);[m
   }[m
   res.json({ message: 'Teks landing page diperbarui', updated: applied });[m
 });[m
[1mdiff --git a/frontend/js/content.js b/frontend/js/content.js[m
[1mindex 96de2e7..324f759 100644[m
[1m--- a/frontend/js/content.js[m
[1m+++ b/frontend/js/content.js[m
[36m@@ -1,6 +1,21 @@[m
 // Dipakai di semua halaman publik (home, team, about) untuk mengisi teks[m
 // yang bisa diedit admin. Elemen dengan id "c-<key>" diisi textContent-nya,[m
 // elemen dengan id "href-<key>" diisi atribut href-nya.[m
[32m+[m
[32m+[m[32m// Cuma izinkan href http/https (atau mailto: untuk email) — jaga-jaga kalau[m
[32m+[m[32m// ada data lama yang tersimpan sebelum validasi backend ditambahkan.[m
[32m+[m[32mfunction isSafeHref(value, allowMailto) {[m
[32m+[m[32m  if (!value) return false;[m
[32m+[m[32m  try {[m
[32m+[m[32m    const url = new URL(value, window.location.href);[m
[32m+[m[32m    if (url.protocol === 'http:' || url.protocol === 'https:') return true;[m
[32m+[m[32m    if (allowMailto && url.protocol === 'mailto:') return true;[m
[32m+[m[32m    return false;[m
[32m+[m[32m  } catch {[m
[32m+[m[32m    return false;[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 (async () => {[m
   try {[m
     const res = await fetch(API_BASE + '/content');[m
[36m@@ -10,7 +25,9 @@[m
       if (textEl) textEl.textContent = value;[m
 [m
       const hrefEl = document.getElementById('href-' + key);[m
[31m-      if (hrefEl) hrefEl.setAttribute('href', value);[m
[32m+[m[32m      if (hrefEl && isSafeHref(value, key === 'contact_email_url')) {[m
[32m+[m[32m        hrefEl.setAttribute('href', value);[m
[32m+[m[32m      }[m
     });[m
   } catch (err) {[m
     console.error('Gagal memuat teks halaman:', err);[m
